{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Category</h1>
    <br/>
    <table class="table">
        <tbody>
            <tr>
                <th>{{ categoryName }}</th>
                <th>{{ category.categoryName }}</th>
            </tr>
            <tr>
                <th>{{ description }}</th>
                <td>{{ category.description }}</td>
            </tr>
        </tbody>
    </table>
    <a href="{{ path('part_new', { 'VenId':vendor.getId(),'CatId':category.getId() }) }}">Go to Parts</a>
        
{% endblock %}

