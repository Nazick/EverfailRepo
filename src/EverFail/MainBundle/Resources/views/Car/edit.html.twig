{% extends '::base.html.twig' %}

{% block body -%}


<div class="form-signin">
    <h1>Car edit</h1>
    <br/>
    {{ form(edit_form) }}
</div>

<ul class="record_actions">
    <li>
        <a href="{{ path('car') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
