{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Vendor List</h1>
    
    <br/>

<table class="table">
	<thead>
		<tr>
			<th>{{ vendorName }}</th>
			<th>{{ vendorEmail }}</th>
			<th>Actions</th>
		</tr>
	</thead>
	<tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('vendor_show', { 'id': entity.id }) }}">{{ entity.vendorName }}</a></td>
                <td>{{ entity.email }}</td>
                <td>
					<ul>
						<li>
							<a href="{{ path('vendor_show', { 'id': entity.id }) }}">show</a>
						</li>
						<li>
							<a href="{{ path('vendor_edit', { 'id': entity.id }) }}">edit</a>
						</li>
					</ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    {% endblock %}
